/**
 * Represents a generic media item (movie, series, documentary).
 * 
 * Interface MediaItem
 */
export interface MediaItem {
  title: string;
  releaseYear: number;
  duration: number;
  genre: string;
}
